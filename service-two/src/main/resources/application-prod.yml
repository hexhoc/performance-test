spring:
  kafka:
    bootstrap-servers: kafka:9092

  datasource:
    url: jdbc:postgresql://postgres:5432/service_two_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
